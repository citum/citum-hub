# Experimental Styles

This directory contains style drafts and compatibility experiments that are not
currently schema-valid against the main CSLN style schema.

Files here are excluded from the default `styles/*.yaml` validation workflow.
Move a style back to `styles/` only after `csln check --style` passes.
